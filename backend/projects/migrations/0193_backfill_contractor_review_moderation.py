# Generated by Codex on 2026-06-08

from django.db import migrations


def backfill_review_moderation(apps, schema_editor):
    ContractorReview = apps.get_model("projects", "ContractorReview")
    rows = ContractorReview.objects.filter(is_verified=True, is_public=True)
    for review in rows.iterator():
        published_at = getattr(review, "published_at", None) or getattr(review, "submitted_at", None) or getattr(review, "created_at", None)
        ContractorReview.objects.filter(pk=review.pk).update(
            moderation_status="approved",
            published_at=published_at,
        )


def noop_reverse(apps, schema_editor):
    pass


class Migration(migrations.Migration):

    dependencies = [
        ("projects", "0192_contractorreview_customer_email_and_more"),
    ]

    operations = [
        migrations.RunPython(backfill_review_moderation, noop_reverse),
    ]
